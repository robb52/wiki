- @articles.each do |article|
  %h2= article.title
  %p
    Published: 
    = article.created_at.strftime('%b %d, %Y')
    %p= truncate(article.content, length: 250)

= link_to "New Article", new_article_path